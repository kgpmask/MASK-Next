import React from "react";
import style from "@/styles/Example.module.css"

const Example = ({text }) => {
	// states and stuff
	// functions
	// hooks, effects and api calls

	return (
		<div className={style.test}>{text}</div>
	);
};

export default Example;